{
    "type": "WebcamoidPluginsCollection",
    "plugins": [
        {
            "name": "FFmpeg screen capture",
            "description": "Capture screen using FFmpeg",
            "id": "VideoSource/DesktopCapture/Impl/FFmpeg",
            "implements": ["DesktopCaptureImpl"],
            "priority": 700,
            "type": "qtplugin"
        }
    ]
}
